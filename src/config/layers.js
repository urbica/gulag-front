import Immutable from 'immutable';

export default Immutable.fromJS([
  {
    id: 'prisons',
    type: 'circle',
    source: 'prisons',
    paint: {
      'circle-radius': 1.75,
      'circle-color': '#ff2b00',
      'circle-opacity': 1
    }
  }
]);
